---
description: Run a command whose output needs full analysis without truncation
---

Use the `slow-commands` skill** to capture the full output of the command.
Then analyze the captured file using normal tools.
